---
description: Inicia o webapp local em localhost
---

Use o terminal para executar exatamente este comando na raiz do projeto:

`npm run dev -- --host localhost`

Depois informe a URL local exibida pelo Vite.
