---
description: Interrompe o webapp local iniciado para testes
---

Use o terminal para encerrar o servidor Vite desta workspace.

No Windows PowerShell, prefira localizar o processo pelo comando associado ao projeto atual e interrompê-lo. Se necessário, use como fallback o processo que estiver ouvindo na porta `5173`.

Depois confirme se o servidor foi encerrado.
